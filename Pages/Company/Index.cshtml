@page
@model CustomerManagementSystem.Pages.Company.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tbl_company[0].company_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tbl_company[0].company_kana)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tbl_company[0].delete_flag)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.tbl_company) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.company_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.company_kana)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.delete_flag)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
